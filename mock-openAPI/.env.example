# copy .env.example .env

HOST=127.0.0.1
PORT=4001

# Must match VAPP_CLIENT_ID / VAPP_CLIENT_SECRET on server/
CLIENT_ID=v-market-dev
CLIENT_SECRET=dev-secret

DATABASE_URL=postgresql+asyncpg://vmarket:vmarket@127.0.0.1:5433/vapp_mock

AUTHCODE_TTL_SECONDS=60
ACCESS_TOKEN_TTL_SECONDS=3600
